# Runtime Evidence

Store redacted runtime, health, capability, performance, and log snapshots here.
Never store keys, passwords, raw user documents, or unredacted prompts.

`installed-baseline.json` is a current-host failure baseline, not a clean-install
acceptance result. It truthfully records the absent Podman reference runtime,
missing ChromaDB, mutable image tags, and unsigned installer.
