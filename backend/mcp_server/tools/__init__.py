"""
MCP Tools Package.

Local-first / desktop-only build: legacy external SaaS connectors (Jira,
Salesforce) have been removed. Tool modules placed here are imported on package
load so their MCP tools register with the registry.
"""
