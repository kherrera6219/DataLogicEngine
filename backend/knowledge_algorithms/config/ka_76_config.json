{
    "exact_matching_keys": ["record_id", "source_sha256"],
    "conflict_resolution_strategy": "reject_conflicting_duplicate",
    "fuzzy_matching_enabled": false,
    "enable_recursive_merging": false
}
