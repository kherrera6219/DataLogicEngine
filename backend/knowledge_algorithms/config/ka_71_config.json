{
    "ingestion_modes": ["batch", "stream"],
    "supported_formats": ["json", "csv", "xml", "parquet"],
    "batch_size": 1000,
    "stream_buffer_ms": 500,
    "handlers": {
        "s3": "S3IngestHandler",
        "kafka": "KafkaStreamHandler",
        "local_file": "FileHandler"
    }
}
